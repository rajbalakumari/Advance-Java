-- Create Database
CREATE DATABASE servlet_db;

-- Use Database
USE servlet_db;

-- Create Table
CREATE TABLE students (
    roll_no INT PRIMARY KEY,
    name VARCHAR(100),
    percentage INT,
    fname VARCHAR(100),
    mother_name VARCHAR(100),
    mail_id VARCHAR(100),
    ph_no VARCHAR(15)
);
